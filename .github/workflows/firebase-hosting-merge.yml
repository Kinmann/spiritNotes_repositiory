# This file was auto-generated by GitHub Actions for Firebase Hosting
name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install and Build
        run: npm ci && npm run build
        working-directory: spirit-notes
        env:
          VITE_FIREBASE_API_KEY: AIzaSyDriRqjMOYFbvmgBxidNoCEU23QGYugGCs
          VITE_FIREBASE_AUTH_DOMAIN: antigravity-dd7c2.firebaseapp.com
          VITE_FIREBASE_PROJECT_ID: antigravity-dd7c2
          VITE_FIREBASE_STORAGE_BUCKET: antigravity-dd7c2.firebasestorage.app
          VITE_FIREBASE_MESSAGING_SENDER_ID: 149534308464
          VITE_FIREBASE_APP_ID: 1:149534308464:web:33e743a318dd8394511cb5
          VITE_FIREBASE_MEASUREMENT_ID: G-BEYLVVGNQ0
      - name: Deploy Functions
        run: |
          # The service account is already available in the environment from the secret
          echo "$FIREBASE_SERVICE_ACCOUNT_FILE" > service_account.json
          export GOOGLE_APPLICATION_CREDENTIALS=$(pwd)/service_account.json
          
          # Install dependencies for functions
          cd functions && npm install && cd ..
          
          # Deploy functions using npx to ensure firebase-tools is available
          npx firebase-tools deploy --only functions --project antigravity-dd7c2 --non-interactive
          
          rm service_account.json
        working-directory: spirit-notes
        env:
          FIREBASE_SERVICE_ACCOUNT_FILE: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_ANTIGRAVITY_DD7C2 }}
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_ANTIGRAVITY_DD7C2 }}'
          channelId: live
          projectId: antigravity-dd7c2
          entryPoint: "spirit-notes"
